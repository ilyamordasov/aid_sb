# aid_sb

React + TypeScript + Vite demo project.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run build:netlify`
- `npm run test`

## Netlify

This repo includes `netlify.toml` with:

- build command: `npm run build:netlify`
- publish dir: `dist`
- SPA redirect to `/index.html`
